# Test configuration: H2 in-memory + visible SQL so the persistence spike
# shows the real INSERT/SELECT round-trip in its output.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=create-drop
